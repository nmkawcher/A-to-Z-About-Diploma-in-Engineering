<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fci.user.allaboutdiplomainengineering">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Introduction" />
        <activity android:name=".whatIsdiploma" />
        <activity android:name=".whydiploma" />
        <activity android:name=".whichInstitute" />
        <activity android:name=".sub" />
        <activity android:name=".admissoninformation" />
        <activity android:name=".cost" />
        <activity android:name=".higerstudy" />
        <activity android:name=".about"></activity>
    </application>

</manifest>